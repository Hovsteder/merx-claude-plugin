# Force install.sh to be detected as Shell -- makes "Shell" the repo's
# detected language (Markdown stays classified as "Prose" by default and
# does not count toward Linguist's language stats). We need *some* real
# language signal so external tools (Tessl, etc.) can scaffold against
# this repo.
install.sh linguist-language=Shell
install.sh linguist-detectable=true
